export const meta = {
  name: 'env-ctl-audit-servermode',
  description: 'Fresh-eyes security audit of the SERVER-MODE remote-edge design before implementation',
  phases: [{ title: 'Audit', detail: 'six design-audit lenses' }, { title: 'Synthesize', detail: 'one AUDIT-server-mode doc' }],
}
const REPO = '/home/drdave/Desktop/env-ctl'
const CTX = `env-ctl SERVER-MODE = a central secretd owns a libSQL vault (sqld on loopback + a pure-Rust remote client); remote thin clients (a Telegram cloud agent, phone, laptop) get relay-bearers-only over HTTPS, DPoP sender-bound; the control plane stays local UDS + SO_PEERCRED, claimed unreachable BY CONSTRUCTION; on-box USB-PARTUUID unlock is the default, the VPS profile is deferred. AUDIT TARGET = the DESIGN in ${REPO}/docs/SERVER-MODE.md (read it fully), cross-referenced with docs/THREAT-MODEL.md, docs/research/03 (libSQL), 12 (token binding), 15 (VPS), docs/ops/01 (systemd), 02 (envctl-component), 07 (ci), and the COMMITTED relay logic in crates/secrets-engine/src/broker/decide.rs. This audits a DESIGN before its Phase 6/8 implementation. READ-ONLY. Be a skeptic: assume a network attacker, a compromised remote or Telegram agent holding a bearer, a stolen bearer, a malicious reverse proxy, and a VPS-snapshot attacker. Cite file and section.`
const FSCHEMA = { type: 'object', additionalProperties: false, properties: { lens: { type: 'string' }, overall: { type: 'string' }, findings: { type: 'array', items: { type: 'object', additionalProperties: false, properties: { severity: { type: 'string', enum: ['critical', 'high', 'medium', 'low', 'info'] }, title: { type: 'string' }, location: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' }, confidence: { type: 'string', enum: ['high', 'medium', 'low'] } }, required: ['severity', 'title', 'issue', 'fix', 'confidence'] } } }, required: ['lens', 'overall', 'findings'] }
const LENSES = [
  `Remote bearer binding (DPoP, RFC 9449): is the sender-constraint sound WITHOUT SO_PEERCRED? Replay across clients, proof reuse and replay window, jkt thumbprint binding, and key custody on a Telegram cloud agent (is the framing that a non-hardware-bound agent is replay-bounded-by-scope-and-TTL honest?). How must decide() change for remote, given the committed decide() peer-binds uid and pid which a remote client lacks?`,
  `Control-plane unreachability BY CONSTRUCTION: is the claim real (separate service objects, control types absent from the edge, a separate process) or hand-waved? Can a remote client ever reach a control RPC? Is the in-process-TLS-only (no TLS-terminating reverse proxy) channel-binding requirement actually enforceable, and what breaks if an operator fronts it with nginx?`,
  `libSQL C-isolation (the ops-flagged blocker): SERVER-MODE claims secretd uses a pure-Rust libsql remote client with no libsql-ffi. The ops doc says the libsql crate default features pull libsql-sys (C) and remote-only purity is non-default and fragile. Adjudicate: is the C-free-secretd claim achievable as written, must it instead use the separate libsql-client and libsql-hrana crates, and what EXACT cargo-tree assertion must gate it?`,
  `Public edge TLS, DoS, and cert separation: the publicly-trusted edge cert versus the disjoint trust roots (edge cert, remote-clients CA, MITM CA, frozen webpki upstream roots), lapsed-cert fail-closed with no MITM-CA fallback, per-IP and per-client rate limiting before crypto, CVE-2024-47609 and write-amplification, and auth-before-swap durable-audit ordering. Any gap that lets a network attacker DoS or downgrade.`,
  `USB-gating versus topology and VPS: the on-box default (USB local, only the relay HTTPS endpoint exposed) versus the deferred VPS profile. Is the PresenceGate abstraction fail-closed for remote (Unproven equals AbsentSince(now))? Is the VPS deferral honest (non-shippable until an operator-authorizer protocol and trusted time exist)? Does a VPS-snapshot attacker get anything the threat model misses?`,
  `Threat-model completeness versus the implemented decide(): walk SERVER-MODE FS-S16 through S26 and REQ-SEC-10 through 13 against the COMMITTED broker decide(). Which server-mode invariants does the current decide() already enforce, which need new clauses (remote binding, channel binding, edge admission), and is anything claimed-enforced actually still unimplemented? List each as ENFORCED, NEEDS-CODE, or GAP.`,
]
phase('Audit')
log('Six design-auditors reviewing the SERVER-MODE remote edge...')
const audits = (await parallel(LENSES.map((l, i) => () =>
  agent(`${CTX}\n\nYOUR LENS:\n${l}\n\nRead the relevant docs and code, then report a verdict plus findings (each with a location, impact, and a concrete fix). Distinguish real design flaws from hardening nits; do not invent issues.`, { label: 'sm-audit:' + i, phase: 'Audit', agentType: 'Explore', schema: FSCHEMA })
))).filter(Boolean)
phase('Synthesize')
const out = await agent(`You are the lead auditor. Dedup and prioritize the six SERVER-MODE lens reports below into ${REPO}/docs/audits/AUDIT-server-mode.md: an executive verdict, a severity-sorted findings table (severity, title, location, impact, fix, confidence), a per-invariant ENFORCED / NEEDS-CODE / GAP table versus the committed decide(), the libSQL C-isolation adjudication with the EXACT required cargo-tree gate, and a resolve-before-Phase-6/8 shortlist. Be honest about the Telegram key-custody residual. After writing, RETURN a JSON object with summary, critical, high, must_resolve, and doc_path.\n\n${CTX}\n\nLENS REPORTS (JSON):\n${JSON.stringify(audits)}`, { label: 'sm-synth', phase: 'Synthesize' })
log('AUDIT-server-mode.md written.')
return out